For project to work:

MUST-HAVE
    - installed python

1: Create virtual environment (Git bash or CMD)
    python -m venv venv

2: Install flask (package manager) when in virtual environment (Git bash or CMD)
    pip install flask

3: ENTER virtual environment:
    CMD:
        venv\Scripts\activate.bat
    Git Bash:
        source venv/Scripts/activate
    EXIT virtual environment (Git bash or CMD):
        deactivate

4: Launch app (Git bash or CMD):
    python app.py

To launch app
CMD or Git Bash:
    python app.py

5: Enjoy!

App features:
- Ability to 
